spring.application.name=device-diagnosis
#server.context-path=/device-diagnosis

# /info
info.groupId=@project.groupId@
info.artifactId=@project.artifactId@
info.version=@project.version@-@buildTime@

# config server
spring.cloud.config.server.name=localhost
spring.cloud.config.server.port=7200
spring.cloud.config.uri=http://${spring.cloud.config.server.name}:${spring.cloud.config.server.port}
# jacksonʱ������
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8

#timeout settings(For debug purpose)
hystrix.command.default.execution.isolation.thread.timeoutInMilliseconds=30000
ribbon.ConnectTimeout=30000
ribbon.ReadTimeout=30000

# �ֲ�ʽ�Ự����
#spring.session.store-type=redis

# ���hystrix��Ч
feign.hystrix.enabled=true

# myBatis
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=org.citic.iiot.diagnosis.domain